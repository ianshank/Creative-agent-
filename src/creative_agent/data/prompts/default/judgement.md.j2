Judgement sweeps — protocol steps 7, 8, 9.

1. Missing-baseline check: for EACH claimed advantage, name the simplest method that
   would plausibly match it. No comparison in the artifact => the advantage is
   unevidenced (emit a finding on the check).
2. Falsifiability calibration: for each prediction, what result would be SURPRISING?
   A threshold below what a null model produces is decoration (above_null=false, with a
   finding).
3. Scope honesty: list every dependency or companion document referenced but not
   supplied; each must be stated as unverified, never assumed sound.

Attach verification_entries for any doctrinal assertions you make.

{{ artifact }}
